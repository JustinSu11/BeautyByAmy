import 'main_beauty.dart' as beauty;

/// Entrypoint wrapper that delegates to `lib/main_beauty.dart` so running
/// `flutter run` (or IDE run) will start the BeautyByAmy app by default.
Future<void> main() => beauty.main();
